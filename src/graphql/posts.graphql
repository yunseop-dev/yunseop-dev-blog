query posts(
  $offset: Int
  $limit: Int
  $orderBy: PostOrder
  $publishedSince: DateTime
  $q: String
) {
  posts(
    offset: $offset
    limit: $limit
    orderBy: $orderBy
    publishedSince: $publishedSince
    q: $q
  ) {
    ...PostField
  }
}
