import styled from "@emotion/styled";

const Logo = styled.div`
  display: flex;
  font-size: 1.5rem;
  font-weight: bold;
  align-items: center;
  & .logo-image {
    width: 60px;
  }
`;

export default Logo;